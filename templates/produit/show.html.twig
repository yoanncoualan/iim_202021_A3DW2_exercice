{% extends 'base.html.twig' %}

{% block body %}
	<h1>{{produit.nom}}</h1>
	<p>{{produit.description}}</p>
	<p>{{produit.prix}}</p>
	<p>{{produit.categorie}}</p>
	<p>{{produit.slug}}</p>
{% endblock %}